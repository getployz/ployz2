# CLI deviations

Record one bullet per intentional difference from the frozen Uncloud command reference, including its reason.

The workspace bootstrap does not declare a command tree, so it introduces no command-shape deviations.
